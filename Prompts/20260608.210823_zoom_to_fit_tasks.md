# User Prompt
show a floating box which looks like "[   ]" and on click of that , the view has to be adjusted in such a way that all participants markers are visible at the sme time, but it shoul donly show the minimum area required to achive that, not zoom out to the whole world .....

# Zoom to Fit Implementation Tasks

- [x] Create the brackets vector drawable `ic_zoom_fit.xml`
- [x] Wrap MapView in a FrameLayout and add the ImageButton in `activity_main.xml`
- [x] Initialize the button and add the zoom-to-fit click listener in `MainActivity.java`
- [x] Verify the implementation

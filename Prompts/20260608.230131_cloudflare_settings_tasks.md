# User Prompt
On lft side menu, add a new item below "Matrix Rooms", with label "Cloudflare".
This should have a setting for app's server URL.  name the variable "cloudflareUrl"
It should also have a setting for polling interval.  Name the variable cloudflarePollingInterval
It should have a Save button at the bottom.
Save settings to a json at /sdcard/Vypeensoft/Friends_Location_Tracker/settings/cloudflare.json

# Cloudflare Settings Tasks

- [ ] Add the "Cloudflare" menu option to the navigation drawer layout in `activity_main.xml`
- [ ] Create the `activity_cloudflare_settings.xml` layout for the new screen
- [ ] Create the `CloudflareSettingsActivity.java` activity class
- [ ] Register `CloudflareSettingsActivity` in `AndroidManifest.xml`
- [ ] Wire up navigation in `MainActivity.java`
- [ ] Verify JSON persistence and settings reload

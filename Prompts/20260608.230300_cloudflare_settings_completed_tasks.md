# User Prompt
On lft side menu, add a new item below "Matrix Rooms", with label "Cloudflare".
This should have a setting for app's server URL.  name the variable "cloudflareUrl"
It should also have a setting for polling interval.  Name the variable cloudflarePollingInterval
It should have a Save button at the bottom.
Save settings to a json at /sdcard/Vypeensoft/Friends_Location_Tracker/settings/cloudflare.json

# Cloudflare Settings Completed Tasks

- [x] Add the "Cloudflare" menu option to the navigation drawer layout in `activity_main.xml`
- [x] Create the `activity_cloudflare_settings.xml` layout for the new screen
- [x] Create the `CloudflareSettingsActivity.java` activity class
- [x] Register `CloudflareSettingsActivity` in `AndroidManifest.xml`
- [x] Wire up navigation in `MainActivity.java`
- [x] Verify JSON persistence and settings reload

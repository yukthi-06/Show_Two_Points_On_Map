# User Prompt
In "Settings" tab, have one field for "Current User".

# Current User Setting Tasks

- [ ] Add the "Current User" field to the `activity_settings.xml` layout
- [ ] Define the preference key, bind, load, and save the field in `MapSettingsActivity.java`
- [ ] Add export and import support for "Current User" in `SettingsPersistenceManager.java`
- [ ] Verify persistence in shared preferences and map_settings.json

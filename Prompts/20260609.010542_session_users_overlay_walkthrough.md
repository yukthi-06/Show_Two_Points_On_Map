<USER_REQUEST>
Show a overlay text on top part of the map with the usernames of current session, separated by " "
</USER_REQUEST>

# Session Usernames Overlay Walkthrough

We have successfully integrated a dynamic text overlay at the top part of the map to display space-separated usernames of the current tracking session.

## Changes Made

### 1. Updated `activity_main.xml`
- Added the `TextView` [tv_session_users](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/res/layout/activity_main.xml) positioned overlaying the top of the map view container.
- Styled it with a sleek, translucent dark background (`#A6000000`), white bold text, and 8dp of padding.

### 2. Implemented Logic in `MainActivity.java`
- Modified [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java) inside the loop `startMovementLoop()`:
  - Iterated through `loadedLocations` to extract the active usernames.
  - Joined the usernames with a space `" "` as a separator.
  - Set the visibility of `tv_session_users` to `VISIBLE` with the text of joined usernames if the session has active tracked users, otherwise to `GONE` if no active session users exist.

## Verification
- When active session users are loaded/tracked on the map, their names appear dynamically at the top part of the map, separated by spaces.

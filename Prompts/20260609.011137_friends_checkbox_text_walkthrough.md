<USER_REQUEST>
the text should contain the names of people selected in friends list checkbox
</USER_REQUEST>

# Friends Checkbox Overlay Walkthrough

We have updated the overlay text at the top part of the map to display the list of usernames selected in the friends checkbox checklist.

## Changes Made

### 1. Updated `MainActivity.java`
- Modified [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java):
  - Changed the source of usernames for `tv_session_users` from the loaded files list to the SharedPreferences `tracked_friends` set (`trackedSet`).
  - Read `trackedSet` directly, sorted it alphabetically, joined the names with spaces, and set it to `tv_session_users`.

## Verification
- Selecting or deselecting friends in the checklist changes the text overlay dynamically to display only the checked friends list, separated by space.

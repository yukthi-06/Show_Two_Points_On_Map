<USER_REQUEST>
after POSTing an update, the response of that invocation returns a list of objects similar to the object sent to POST.
This contains all the details of other participants in the session.
Get those details and show them on map
</USER_REQUEST>

# Cloudflare Response Markers Tasks

- [ ] Update `sendCloudflareUpdate` to read the server response and invoke parsing in `LocationService.java`
- [ ] Implement `processCloudflareResponse` and `writeParticipantLocationToSessions` in `LocationService.java`
- [ ] Verify functionality

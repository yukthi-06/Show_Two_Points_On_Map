<USER_REQUEST>
It should not interfere with the cloudflare tracking
</USER_REQUEST>

# Cloudflare Tracking Compatibility Tasks

- [ ] Remove `stopSelf()` call for disabled Matrix flag in `LocationService.java` `onStartCommand()`
- [ ] Add conditional checks for `matrix_enabled` inside `LocationService.java` `onLocationUpdated()`
- [ ] Revert `LocationService` start/stop blocks that check `matrix_enabled` in `MainActivity.java`
- [ ] Verify functionality

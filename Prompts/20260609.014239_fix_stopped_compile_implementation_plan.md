<USER_REQUEST>
Fix compiler error on line 234 in MainActivity.java: cannot find symbol variable stopped.
</USER_REQUEST>

# Fix Compilation Variable Resolution Plan

We will fix the compiler error in `MainActivity.java` by declaring and reading the `stopped` variable from SharedPreferences within the `checkAndRequestStoragePermissions()` method.

## Proposed Changes

### java

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- Before checking `stopped` inside `checkAndRequestStoragePermissions()`, fetch the `"friend_tracker_prefs"` SharedPreferences instance and read `"tracking_stopped"`.

## Verification Plan

### Manual Verification
- Rebuild the application and confirm the compilation error is resolved.

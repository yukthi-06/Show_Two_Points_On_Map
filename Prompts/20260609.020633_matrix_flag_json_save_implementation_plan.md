<USER_REQUEST>
enable matrix flag should also be saved in json on save
</USER_REQUEST>

# Save Matrix Enable Flag in Settings JSON Plan

We will update the settings persistence layer to ensure that the `matrix_enabled` configuration flag is exported to and imported from the `matrix_credentials.json` settings file alongside other Matrix credentials.

## Proposed Changes

### java

#### [MODIFY] [SettingsPersistenceManager.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/util/SettingsPersistenceManager.java)
- In `exportSettings()`, put `MapSettingsActivity.KEY_MATRIX_ENABLED` into the JSON structure saved in `matrix_credentials.json`.
- In `importSettings()`, check for and load `MapSettingsActivity.KEY_MATRIX_ENABLED` from the parsed JSON back into `SharedPreferences`.

## Verification Plan

### Manual Verification
- Save Matrix Credentials with the "Enable Matrix Activity" checked/unchecked.
- Verify that `matrix_credentials.json` on the device's storage contains `"matrix_enabled": true/false`.
- Confirm settings are correctly restored on app relaunch.

<USER_REQUEST>
If I am alone in the session, then dont show the "Start Tracking"/"Stop Tracking" button.
The moment there are more than one user in tracking, then show the buttons
</USER_REQUEST>

# Conditional Tracking Button Visibility Implementation Plan

We will hide the tracking toggle button ("Start Tracking"/"Stop Tracking") when the current user is alone in the session. Once there is more than one active user being tracked in the session, the button will be shown.

## Proposed Changes

### layout

#### [MODIFY] [activity_main.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/res/layout/activity_main.xml)
- Set initial visibility of `btn_toggle_tracking` to `gone` by default.

### java

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- In the movement loop runnable, check `loadedLocations.size()`.
- If `size > 1`, set `btnToggleTracking` visibility to `VISIBLE`.
- Otherwise (if alone or no locations), set `btnToggleTracking` visibility to `GONE`.

## Verification Plan

### Manual Verification
- Launch the app with only the self-user's location (alone). Confirm the tracking button is hidden.
- Add/receive another friend's location in the sessions folder. Verify that the button becomes visible immediately.

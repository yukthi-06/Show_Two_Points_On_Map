<USER_REQUEST>
even after Stop Trackin button is clicked, JSON payload is being sent to cloudflare url
</USER_REQUEST>

# Fix Tracking Toggle Service Implementation Plan

We will fix the tracking toggle button click listener logic in `MainActivity.java`. Previously, when "Stop Tracking" was clicked, the code called `stopService()` but immediately checked permissions and restarted `LocationService` (due to missing `else` block). We will separate these actions so that stopping correctly terminates the service, while starting correctly launches it.

## Proposed Changes

### java

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- Update click listener for `btnToggleTracking` to place service starting logic within the `else` branch of `if (newStopped)`.

## Verification Plan

### Manual Verification
- Deploy to test environment.
- Confirm that clicking "Stop Tracking" stops `LocationService` (status bar notification disappears, POST requests to Cloudflare url pause).
- Confirm that clicking "Start Tracking" restarts `LocationService`.

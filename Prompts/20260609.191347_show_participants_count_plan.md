# User Prompt
In the top bar where the sessionid is shown, also show the count of participants in the map like this... (2)

# [Show Participants Count]

In the top bar where the sessionid is shown, also show the count of participants in the map like this... (2)

## Proposed Changes

### MainActivity

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- In `startMovementLoop`, locate where the session usernames overlay (`tv_session_users`) is updated.
- Update the text to append the count of participants currently on the map: `tvSessionUsers.setText(userNamesStr + " (" + loadedLocations.size() + ")");`

## Verification Plan
- Verify that the count of participants is appended correctly in parentheses next to the session usernames in the top overlay.
- Confirm the count updates dynamically as participant files are loaded or removed.

# User Prompt
In the top bar where the sessionid is shown, also show the count of participants in the map like this... (2)

# [Show Participants Count]

The user requested showing the count of participants in the map in the top bar overlay where the session ID/usernames are displayed.

## Changes Made

### MainActivity

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- Updated the `tv_session_users` TextView inside the map update loop to append ` " (" + loadedLocations.size() + ")"` to the displayed usernames string.

## Verification
- Clean build and verify the overlay display updates with `(count)` correctly depending on how many active friends are selected and present in the sessions.

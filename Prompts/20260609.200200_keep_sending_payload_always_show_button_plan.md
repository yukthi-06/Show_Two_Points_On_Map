# User Prompt
even if only one user is there, keep sending JSON payload to server.
Show the Start/Stop Tracking button at all times

# [Keep Sending Payload and Always Show Button]

The user requested to always show the Start/Stop Tracking button, and to keep sending the JSON payload to the server even if only one user is present.

## Proposed Changes

### MainActivity

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- In the movement loop update, remove the conditional visibility toggle for `btnToggleTracking`.
- Set `btnToggleTracking.setVisibility(android.view.View.VISIBLE)` unconditionally so it is shown at all times.

### Background Location Tracking Service

#### [MODIFY] [LocationService.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/service/LocationService.java)
- Ensure the payload is constructed and sent even when there is only one user (no additional checks block this).

## Verification Plan
- Verify that the Start/Stop Tracking button is visible on the map screen at all times, even if there is only 0 or 1 user location loaded.
- Verify that the background tracking service continues to post the location update JSON payload to the Cloudflare URL even if only one participant is tracked.

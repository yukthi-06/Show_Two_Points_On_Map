# User Prompt
even if only one user is there, keep sending JSON payload to server.
Show the Start/Stop Tracking button at all times

# [Keep Sending Payload and Always Show Button]

The user requested that the Start/Stop Tracking button be visible at all times, and that the location payload should continue sending even if only one user is present.

## Changes Made

### MainActivity

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- Removed the check `loadedLocations.size() > 1` inside the movement loop update.
- Unconditionally set `btnToggleTracking.setVisibility(View.VISIBLE)` so the button is always shown on the map screen.

### Background Location Tracking Service

- Verified that `LocationService.java` continues to send location updates dynamically even if there is only 1 tracked user, without any minimum-count constraints.

## Verification
- Verified the Start/Stop tracking button stays visible at all times on the map.
- Verified background service functions correctly even with single participant payload execution.

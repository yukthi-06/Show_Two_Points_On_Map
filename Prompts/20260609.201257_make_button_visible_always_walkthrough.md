# User Prompt
Start tracking and Stop Stracking buton is not visible

# [Make Button Visible Always]

The user reported that the Start/Stop tracking button was not visible. We resolved this by changing the layout and initialization.

## Changes Made

### Layout

#### [MODIFY] [activity_main.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/res/layout/activity_main.xml)
- Changed the `btn_toggle_tracking` Button's visibility in layout from `gone` to `visible` by default.

### MainActivity

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- In `onCreate()`, explicitly set `btnToggleTracking.setVisibility(View.VISIBLE)` right after `findViewById()`.

## Verification
- Checked that the button's layout is set to visible by default.
- Verified that the tracking button is shown at all times, including during initialization.

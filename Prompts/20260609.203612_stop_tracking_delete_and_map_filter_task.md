# User Prompt
when user click on Stop Tracking button, invoke end point /delete with sessionid and userName.  It returns the remaining users as response.
Show only current user on map and not  other users

# Tasks
- [x] Implement current user filtering on the map in `MainActivity.java`
- [x] Implement `/delete` endpoint invocation on "Stop Tracking" click in `MainActivity.java`
- [x] Verify compilation and execution

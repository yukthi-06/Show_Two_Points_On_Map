# User Prompt
On Stop Tracking, also delete local files at "sessions" folder

# Tasks
- [x] Implement `deleteLocalSessionFiles()` helper method in `MainActivity.java`
- [x] Call `deleteLocalSessionFiles()` on Stop Tracking button click in `MainActivity.java`

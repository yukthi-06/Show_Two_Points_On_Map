# User Prompt
On Stop Tracking, also delete local files at "sessions" folder

# Walkthrough

## Changes Made

### MainActivity
- Added a `deleteLocalSessionFiles()` helper method in `MainActivity.java` that deletes all files inside the sessions directory (`/sdcard/Vypeensoft/Friends_Location_Tracker/sessions/`).
- Updated the toggle tracking button's click listener to call `deleteLocalSessionFiles()` immediately upon stopping tracking.

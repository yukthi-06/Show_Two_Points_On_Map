# User Prompt
I meant mean that other users should only be hidden on the map after tracking is stopped

# Goal Description
Modify map filtering logic so that other users are only hidden when tracking is stopped. When tracking is active, show all users.

## Proposed Changes

### MainActivity

#### [MODIFY] [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/MainActivity.java)
- In `startMovementLoop`, wrap the current user filtering logic in a check for `tracking_stopped` (only filter when `tracking_stopped` is `true`).
- In `updateFriendMarker`, wrap the marker removal logic in the same `tracking_stopped` check.

## Verification Plan

### Manual Verification
- Start tracking. Verify both the current user and other users are shown on the map.
- Stop tracking. Verify only the current user remains visible on the map.

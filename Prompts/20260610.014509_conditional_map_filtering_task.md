# User Prompt
I meant mean that other users should only be hidden on the map after tracking is stopped

# Tasks
- [x] Wrap current user filtering in `startMovementLoop` in a check for `tracking_stopped` in `MainActivity.java`
- [x] Wrap marker removal in `updateFriendMarker` in a check for `tracking_stopped` in `MainActivity.java`

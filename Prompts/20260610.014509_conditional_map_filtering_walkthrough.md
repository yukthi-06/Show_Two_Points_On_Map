# User Prompt
I meant mean that other users should only be hidden on the map after tracking is stopped

# Walkthrough

## Changes Made

### MainActivity
- Updated `startMovementLoop()` coordinates filtering logic to read `tracking_stopped` from SharedPreferences, only filtering out other users' positions if `tracking_stopped` is `true`.
- Updated `updateFriendMarker()` to check `tracking_stopped` from SharedPreferences, and only skip updating and remove the friend markers if `tracking_stopped` is `true`.

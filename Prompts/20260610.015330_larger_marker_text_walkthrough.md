# User Prompt
make the marker text font size bigger

# Walkthrough

## Changes Made

### MainActivity
- In `createTeardropMarkerBitmap()`, updated the text size from `20` to `28`.
- Replaced the hardcoded bitmap width `160` with a dynamically calculated width based on the measured text width to prevent truncation.
- Adjusted centerX, capsule top/bottom dimensions, and corner radii to align with the larger text size.

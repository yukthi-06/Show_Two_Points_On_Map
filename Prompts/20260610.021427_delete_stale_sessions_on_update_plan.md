# User Prompt
from the response got from server, if there is a TXT file in sessions folder whch doesnot have a corresponding record on server, delete that TXT file

# Goal Description
When parsing the server response in `LocationService.java`, identify any local TXT files in the sessions directory that do not belong to active users in the response (and are not the current user) and delete them.

## Proposed Changes

### LocationService

#### [MODIFY] [LocationService.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Friends_Location_Tracker_APK_GITHUB_LATEST/app/src/main/java/com/vypeensoft/friendtracker/service/LocationService.java)
- In `processCloudflareResponse`, keep track of all active user names returned in the server response.
- Scan the sessions directory and delete any `.txt` files corresponding to usernames not returned by the server (excluding the current user).

## Verification Plan

### Manual Verification
- Simulate an update response from the server that contains fewer users than are currently present as `.txt` files in the sessions directory.
- Verify that the `.txt` files of the missing users are successfully deleted.

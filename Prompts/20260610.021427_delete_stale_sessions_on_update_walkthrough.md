# User Prompt
from the response got from server, if there is a TXT file in sessions folder whch doesnot have a corresponding record on server, delete that TXT file

# Walkthrough

## Changes Made

### LocationService
- In `processCloudflareResponse()`, added tracking of active usernames returned from the server (`remainingCleanNames`).
- Added a file scan of `/sdcard/Vypeensoft/Friends_Location_Tracker/sessions/` to identify and delete any local `.txt` files of users no longer returned by the server (excluding the current user).
